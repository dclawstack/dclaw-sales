apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-sales.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-sales.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "dclaw-sales.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
